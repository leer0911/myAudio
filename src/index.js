import './scss/index.scss';
import config from './config';

/**
 * @param  {Element|Object} el
 * @param  {Object} config
 */
const myAudio = (el, opt) => {
  let myaudio = this;
  let timers = {};
  let api;

  // Build the default HTML
  function _buildControls() {}
  function init() {
    _buildControls();
  }
  init();
  return api;
};

myAudio();
